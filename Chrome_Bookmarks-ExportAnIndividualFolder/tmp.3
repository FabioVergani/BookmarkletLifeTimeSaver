(w=>{
	const d=w.document;
	if('bookmarks'===d.domain){
		const ƒ=loaded=>{
			let e=loaded.target.getElementById('bookmarks-app');
			if(null!==e){
				(e.dump||(e.dump=(e=>{
					let dumpAllowed=true;
					//
					//const sideBar=e.querySelector('#sidebar');
					//console.dir(sideBar)
					console.dir(e)
					//..
					return e=>{
						if(dumpAllowed){
							dumpAllowed=false;

								//console.dir(e.$)

							dumpAllowed=true
						}
					}
				})(e)))(e)
			}
		},
		onceAt=(e,s,c)=>{let f=o=>{e.removeEventListener(s,f);f=null;c(o)};e.addEventListener(s,f)};
		if('complete'!==d.readyState){onceAt(w,'load',ƒ)}else{ƒ({target:d})}
	}
})(window);
